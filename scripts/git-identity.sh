#!/usr/bin/env bash
# 本仓库 git 提交身份（仅 local，不修改 global）
GIT_USER_NAME="vincisgao"
GIT_USER_EMAIL="vincisgao@gmail.com"

git config user.name "${GIT_USER_NAME}"
git config user.email "${GIT_USER_EMAIL}"
