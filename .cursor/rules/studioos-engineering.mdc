---
description: VINCIS Development Bible — 工程铁律与 Feature First 架构
alwaysApply: true
---

# VINCIS Engineering Rules

参考 `docs/STUDIOOS_ROADMAP.md`、`docs/VINCIS_ORDER_LIFECYCLE_SPEC.md` 与开发者文档 Bible。

## 核心原则

- **Campaign 是唯一 Aggregate Root** — 新表必须含 `campaign_id`
- **Feature First** — 业务放 `features/{name}/`，禁止 `misc/`、`helpers2/`
- **分层** — Page → Action → Service → Repository → Prisma（禁止跳层）
- **状态机** — 禁止 `status = "APPROVED"`，必须 `StateMachine.transition()`
- **事件驱动** — 副作用（邮件/通知/日志）监听 Event，不在 Action 里 inline
- **订单生命周期不可改写** — 涉及发布、付款、匹配、邀约、选定、项目生成、审片、加购、结算时，必须严格遵守 `docs/VINCIS_ORDER_LIFECYCLE_SPEC.md`
- **真实 AI 匹配邀约** — 真实 Campaign 的邀约必须来自 frozen production brief + 已注册创作者真实资料 + AI 学习记忆的匹配结果；禁止伪造候选人或 demo fallback

## 订单 / 邀约 / 项目闭环

- `docs/VINCIS_ORDER_LIFECYCLE_SPEC.md` 中“中文最高优先级流程”是订单生命周期唯一业务真相
- 发布只进入托管付款；**付款成功后**才允许 AI 匹配创作者并发送邀约
- 广告主选中 AI 创意后进入付款；付款成功后进入匹配阶段，不能直接进入制作
- AI 生成 3 个创意、广告主选择创意、拒绝邀约、广告主不选 AI 推荐人，都必须写入 AI 学习
- 邀约必须是 AI / matching service 对已注册创作者的真实匹配结果；上线冷启动阶段可优先邀约测试/注册创作者，但不得伪造不存在的候选人
- 用户和履约数据增长后，匹配必须逐步以作品、偏好、接受/拒绝历史、履约表现、评分、Relationship DNA 等数据为准
- 创作者接受邀约只代表「有合作意向」，**绝不能**生成正式项目 / 订单 / 上传入口
- 创作者拒绝邀约必须填写拒绝理由；拒绝理由必须进入 AI 行为学习
- 创作者接受或拒绝邀约后，广告主都必须收到通知
- 只有品牌从已接受邀约中明确「最终选定」创作者后，才生成正式 creator project / order
- 被选定创作者必须收到恭喜动画通知；文案必须表达「已经正式达成合作」
- 被选定创作者收到恭喜 / 项目就绪通知后，才可在工作台看到项目表单并直接进入审片中心上传 V1
- 品牌未选择前，创作者工作台不得出现正式项目卡、订单卡或 V1 上传入口
- 创作者上传每一版，广告主必须收到推送；广告主要求修改 / 通过 / 释放款项 / 加付 20% 后，创作者必须收到推送
- V1–V5 与第 1–5 轮修订一一对应；V1–V3 免费；一次 20% 加购解锁 V4–V5；V5 后仍未通过必须平台介入
- V1、V2、V3、V4、V5 任意版本只要广告主点击通过，系统就释放款项并结束项目
- 禁止为了修 bug 或简化状态而绕开上述闭环；除非项目 owner 明确要求修改 `docs/VINCIS_ORDER_LIFECYCLE_SPEC.md`

## 文件限制

- React 组件 ≤ 300 行
- 单文件 ≤ 500 行
- Hook ≤ 200 行
- 禁止 `any`、`@ts-ignore`

## Review 模块

- 品牌：评论、标注、Approve / Need Revision
- 创作者：只读 + 上传新版本；**待品牌审核且品牌尚未批注**时可「退回上传」，覆盖当前稿后重新提交（`CREATOR_REVERT_UPLOAD` / `CREATOR_REVERT`）
- **退回上传锁定**：品牌方一旦进入批注流程（`ReviewStatus.REVIEWING` 或已有 Brand 批注/标注），创作者不可再退回上传
- 坐标用百分比，禁止像素
- **审片版本 / 修订轮次**：V1–V5 与第 1–5 轮修订 **一一对应**（Vn = 第 n 轮）；前 3 轮免费；**一次加购（项目金额 20%）解锁第 4–5 轮**（`paidRevisionSlotsUnlocked >= 1`）；第 5 轮仍不过审 → 平台介入（见 `review-round-policy.ts`）
- 组件拆分：ReviewPlayer / Timeline / AnnotationCanvas / CommentPanel 独立

## 禁止

- 页面直接 fetch 数据库 / Prisma
- 业务逻辑写在 UI 组件
- 直接修改 Wallet 余额
- 前端调用 OpenAI
- console.log（用 `lib/core/logger`）
- 把「接受邀约」当成「项目生成」
- 把「接受邀约」当成「正式达成合作」
- 未付款就创建 creator invitations
- 未品牌最终选定就创建 creator order/project
- 未正式达成合作就在创作者工作台展示项目表单或 V1 上传入口
- 在真实 Campaign 中使用 demo fallback / placeholder shortlist 伪造邀约；测试账号必须是真实注册 creator account
