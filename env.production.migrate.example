# Rename/copy to .env.production.migrate in project root (gitignored).
# Vercel → POSTGRES_URL_NON_POOLING → copy full value. Host must NOT contain "-pooler".
POSTGRES_URL_NON_POOLING=postgresql://USER:PASSWORD@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require
